// Placeholder OCR service
// Implementação futura: integrar Tesseract, Google Vision, or OCR via Ollama pipeline.

export async function extractTextFromImage(filePath: string): Promise<string> {
  // Atualmente não implementado — retornar placeholder
  return Promise.resolve('OCR not implemented.');
}
